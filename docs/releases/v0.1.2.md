# v0.1.2 Hotfix

## Fixed

- Improved login form compatibility when the forum returns alternate username, password, submit, and redirect field names.
- Added verification code handling to the login screen so captcha-protected login flows can be completed.
- Preserved clearer login failure messages from forum responses instead of falling back to generic errors.

## Release

- Android APK/AAB and Windows x64 packages are built by the release workflow when pushing tag `v0.1.2`.
